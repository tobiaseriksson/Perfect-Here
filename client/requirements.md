## Packages
date-fns | Date manipulation for calendar grid
framer-motion | Smooth animations for dialogs and page transitions
clsx | Conditional class names
tailwind-merge | Merging tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  comic: ["'Architects Daughter', cursive"],
  sans: ["'DM Sans', sans-serif"],
}
Authentication handled via Replit Auth (use-auth.ts hook)
